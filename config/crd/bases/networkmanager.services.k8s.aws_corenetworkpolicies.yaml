---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: corenetworkpolicies.networkmanager.services.k8s.aws
spec:
  group: networkmanager.services.k8s.aws
  names:
    kind: CoreNetworkPolicy
    listKind: CoreNetworkPolicyList
    plural: corenetworkpolicies
    singular: corenetworkpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.coreNetworkID
      name: CORENETWORKID
      type: string
    - jsonPath: .status.policyVersionID
      name: POLICYVERSION
      type: string
    - jsonPath: .status.changeSetState
      name: CHANGESETSTATE
      type: string
    - jsonPath: .status.conditions[?(@.type=="ACK.ResourceSynced")].status
      name: Synced
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CoreNetworkPolicy is the Schema for the CoreNetworkPolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              CoreNetworkPolicySpec defines the desired state of CoreNetworkPolicy.

              Describes a core network policy. You can have only one LIVE Core Policy.
            properties:
              autoExecute:
                type: boolean
              clientToken:
                description: |-
                  The client token associated with the request.

                  Regex Pattern: `^[\s\S]*$`
                type: string
              coreNetworkID:
                description: |-
                  The ID of a core network.

                  Regex Pattern: `^core-network-([0-9a-f]{8,17})$`
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              description:
                description: |-
                  a core network policy description.

                  Regex Pattern: `^[\s\S]*$`
                type: string
              latestVersionID:
                description: The ID of a core network policy.
                format: int64
                type: integer
              policyDocument:
                type: string
            required:
            - coreNetworkID
            - policyDocument
            type: object
          status:
            description: CoreNetworkPolicyStatus defines the observed state of CoreNetworkPolicy
            properties:
              ackResourceMetadata:
                description: |-
                  All CRs managed by ACK have a common `Status.ACKResourceMetadata` member
                  that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: |-
                      ARN is the Amazon Resource Name for the resource. This is a
                      globally-unique identifier and is set only by the ACK service controller
                      once the controller has orchestrated the creation of the resource OR
                      when it has verified that an "adopted" resource (a resource where the
                      ARN annotation was set by the Kubernetes user on the CR) exists and
                      matches the supplied CR's Spec field values.
                      https://github.com/aws/aws-controllers-k8s/issues/270
                    type: string
                  ownerAccountID:
                    description: |-
                      OwnerAccountID is the AWS Account ID of the account that owns the
                      backend AWS service API resource.
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists
                      or will exist.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              alias:
                description: |-
                  Whether a core network policy is the current LIVE policy or the most recently
                  submitted policy.
                type: string
              changeSetState:
                description: The state of a core network policy.
                type: string
              conditions:
                description: |-
                  All CRs managed by ACK have a common `Status.Conditions` member that
                  contains a collection of `ackv1alpha1.Condition` objects that describe
                  the various terminal states of the CR and its backend AWS service API
                  resource
                items:
                  description: |-
                    Condition is the common struct used by all CRDs managed by ACK service
                    controllers to indicate terminal states  of the CR and its backend AWS
                    service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: The timestamp when a core network policy was created.
                format: date-time
                type: string
              policyErrors:
                description: Describes any errors in a core network policy.
                items:
                  description: Provides details about an error in a core network policy.
                  properties:
                    errorCode:
                      type: string
                    message:
                      type: string
                    path:
                      type: string
                  type: object
                type: array
              policyVersionID:
                description: The ID of the policy version.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
