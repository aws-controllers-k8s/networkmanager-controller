---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: sitetositevpnattachments.networkmanager.services.k8s.aws
spec:
  group: networkmanager.services.k8s.aws
  names:
    kind: SiteToSiteVPNAttachment
    listKind: SiteToSiteVPNAttachmentList
    plural: sitetositevpnattachments
    singular: sitetositevpnattachment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.attachmentID
      name: ATTACHMENTID
      type: string
    - jsonPath: .spec.coreNetworkID
      name: CORENETWORK
      priority: 1
      type: string
    - jsonPath: .status.state
      name: STATE
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SiteToSiteVPNAttachment is the Schema for the SiteToSiteVPNAttachments
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              SiteToSiteVpnAttachmentSpec defines the desired state of SiteToSiteVpnAttachment.

              Creates a site-to-site VPN attachment.
            properties:
              coreNetworkID:
                description: |-
                  The ID of a core network where you're creating a site-to-site VPN attachment.

                  Regex Pattern: `^core-network-([0-9a-f]{8,17})$`
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              routingPolicyLabel:
                description: |-
                  The routing policy label to apply to the Site-to-Site VPN attachment for
                  traffic routing decisions.

                  Regex Pattern: `^[\s\S]*$`
                type: string
              tags:
                description: The tags associated with the request.
                items:
                  description: Describes a tag.
                  properties:
                    key:
                      type: string
                    value:
                      type: string
                  type: object
                type: array
              vpnConnectionARN:
                description: |-
                  The ARN identifying the VPN attachment.

                  Regex Pattern: `^arn:[^:]{1,63}:ec2:[^:]{0,63}:[^:]{0,63}:vpn-connection\/vpn-[0-9a-f]{8,17}$`
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
            required:
            - coreNetworkID
            - vpnConnectionARN
            type: object
          status:
            description: SiteToSiteVPNAttachmentStatus defines the observed state
              of SiteToSiteVPNAttachment
            properties:
              ackResourceMetadata:
                description: |-
                  All CRs managed by ACK have a common `Status.ACKResourceMetadata` member
                  that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: |-
                      ARN is the Amazon Resource Name for the resource. This is a
                      globally-unique identifier and is set only by the ACK service controller
                      once the controller has orchestrated the creation of the resource OR
                      when it has verified that an "adopted" resource (a resource where the
                      ARN annotation was set by the Kubernetes user on the CR) exists and
                      matches the supplied CR's Spec field values.
                      https://github.com/aws/aws-controllers-k8s/issues/270
                    type: string
                  ownerAccountID:
                    description: |-
                      OwnerAccountID is the AWS Account ID of the account that owns the
                      backend AWS service API resource.
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists
                      or will exist.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              attachment:
                description: Provides details about a site-to-site VPN attachment.
                properties:
                  attachmentID:
                    type: string
                  attachmentPolicyRuleNumber:
                    format: int64
                    type: integer
                  attachmentType:
                    type: string
                  coreNetworkARN:
                    type: string
                  coreNetworkID:
                    type: string
                  createdAt:
                    format: date-time
                    type: string
                  edgeLocation:
                    type: string
                  edgeLocations:
                    items:
                      type: string
                    type: array
                  lastModificationErrors:
                    items:
                      description: Describes the error associated with an attachment
                        request.
                      properties:
                        code:
                          type: string
                        message:
                          type: string
                        requestID:
                          type: string
                        resourceARN:
                          type: string
                      type: object
                    type: array
                  networkFunctionGroupName:
                    type: string
                  ownerAccountID:
                    type: string
                  proposedNetworkFunctionGroupChange:
                    description: Describes proposed changes to a network function
                      group.
                    properties:
                      attachmentPolicyRuleNumber:
                        format: int64
                        type: integer
                      networkFunctionGroupName:
                        type: string
                      tags:
                        items:
                          description: Describes a tag.
                          properties:
                            key:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                    type: object
                  proposedSegmentChange:
                    description: |-
                      Describes a proposed segment change. In some cases, the segment change must
                      first be evaluated and accepted.
                    properties:
                      attachmentPolicyRuleNumber:
                        format: int64
                        type: integer
                      segmentName:
                        type: string
                      tags:
                        items:
                          description: Describes a tag.
                          properties:
                            key:
                              type: string
                            value:
                              type: string
                          type: object
                        type: array
                    type: object
                  resourceARN:
                    type: string
                  segmentName:
                    type: string
                  state:
                    type: string
                  tags:
                    items:
                      description: Describes a tag.
                      properties:
                        key:
                          type: string
                        value:
                          type: string
                      type: object
                    type: array
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              attachmentID:
                description: |-
                  The ID of the attachment.

                  Regex Pattern: `^attachment-([0-9a-f]{8,17})$`
                type: string
              conditions:
                description: |-
                  All CRs managed by ACK have a common `Status.Conditions` member that
                  contains a collection of `ackv1alpha1.Condition` objects that describe
                  the various terminal states of the CR and its backend AWS service API
                  resource
                items:
                  description: |-
                    Condition is the common struct used by all CRDs managed by ACK service
                    controllers to indicate terminal states  of the CR and its backend AWS
                    service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              state:
                description: The state of the attachment.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
