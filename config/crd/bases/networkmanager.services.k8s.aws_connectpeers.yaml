---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: connectpeers.networkmanager.services.k8s.aws
spec:
  group: networkmanager.services.k8s.aws
  names:
    kind: ConnectPeer
    listKind: ConnectPeerList
    plural: connectpeers
    singular: connectpeer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: STATE
      type: string
    - jsonPath: .spec.connectAttachmentID
      name: CONNECTATTACHMENT
      type: string
    - jsonPath: .status.conditions[?(@.type=="ACK.ResourceSynced")].status
      name: Synced
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConnectPeer is the Schema for the ConnectPeers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ConnectPeerSpec defines the desired state of ConnectPeer.

              Describes a core network Connect peer.
            properties:
              bgpOptions:
                description: |-
                  The Connect peer BGP options. This only applies only when the protocol is
                  GRE.
                properties:
                  peerASN:
                    format: int64
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              connectAttachmentID:
                description: |-
                  The ID of the connection attachment.

                  Regex Pattern: `^attachment-([0-9a-f]{8,17})$`
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              coreNetworkAddress:
                description: |-
                  A Connect peer core network address. This only applies only when the protocol
                  is GRE.

                  Regex Pattern: `^[\s\S]*$`
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              insideCIDRBlocks:
                description: The inside IP addresses used for BGP peering.
                items:
                  type: string
                type: array
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              peerAddress:
                description: |-
                  The Connect peer address.

                  Regex Pattern: `^[\s\S]*$`
                type: string
                x-kubernetes-validations:
                - message: Value is immutable once set
                  rule: self == oldSelf
              subnetARN:
                description: |-
                  The subnet ARN for the Connect peer. This only applies only when the protocol
                  is NO_ENCAP.

                  Regex Pattern: `^arn:[^:]{1,63}:ec2:[^:]{0,63}:[^:]{0,63}:subnet\/subnet-[0-9a-f]{8,17}$|^$`
                type: string
              tags:
                description: The tags associated with the peer request.
                items:
                  description: Describes a tag.
                  properties:
                    key:
                      type: string
                    value:
                      type: string
                  type: object
                type: array
            required:
            - connectAttachmentID
            - peerAddress
            type: object
          status:
            description: ConnectPeerStatus defines the observed state of ConnectPeer
            properties:
              ackResourceMetadata:
                description: |-
                  All CRs managed by ACK have a common `Status.ACKResourceMetadata` member
                  that is used to contain resource sync state, account ownership,
                  constructed ARN for the resource
                properties:
                  arn:
                    description: |-
                      ARN is the Amazon Resource Name for the resource. This is a
                      globally-unique identifier and is set only by the ACK service controller
                      once the controller has orchestrated the creation of the resource OR
                      when it has verified that an "adopted" resource (a resource where the
                      ARN annotation was set by the Kubernetes user on the CR) exists and
                      matches the supplied CR's Spec field values.
                      https://github.com/aws/aws-controllers-k8s/issues/270
                    type: string
                  ownerAccountID:
                    description: |-
                      OwnerAccountID is the AWS Account ID of the account that owns the
                      backend AWS service API resource.
                    type: string
                  region:
                    description: Region is the AWS region in which the resource exists
                      or will exist.
                    type: string
                required:
                - ownerAccountID
                - region
                type: object
              conditions:
                description: |-
                  All CRs managed by ACK have a common `Status.Conditions` member that
                  contains a collection of `ackv1alpha1.Condition` objects that describe
                  the various terminal states of the CR and its backend AWS service API
                  resource
                items:
                  description: |-
                    Condition is the common struct used by all CRDs managed by ACK service
                    controllers to indicate terminal states  of the CR and its backend AWS
                    service API resource
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the Condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              configuration:
                description: The configuration of the Connect peer.
                properties:
                  bgpConfigurations:
                    items:
                      description: Describes a core network BGP configuration.
                      properties:
                        coreNetworkASN:
                          format: int64
                          type: integer
                        coreNetworkAddress:
                          type: string
                        peerASN:
                          format: int64
                          type: integer
                        peerAddress:
                          type: string
                      type: object
                    type: array
                  coreNetworkAddress:
                    type: string
                  insideCIDRBlocks:
                    items:
                      type: string
                    type: array
                  peerAddress:
                    type: string
                  protocol:
                    type: string
                type: object
              connectPeerID:
                description: |-
                  The ID of the Connect peer.

                  Regex Pattern: `^connect-peer-([0-9a-f]{8,17})$`
                type: string
              coreNetworkID:
                description: |-
                  The ID of a core network.

                  Regex Pattern: `^core-network-([0-9a-f]{8,17})$`
                type: string
              createdAt:
                description: The timestamp when the Connect peer was created.
                format: date-time
                type: string
              edgeLocation:
                description: |-
                  The Connect peer Regions where edges are located.

                  Regex Pattern: `^[\s\S]*$`
                type: string
              lastModificationErrors:
                description: Describes the error associated with the attachment request.
                items:
                  description: Describes an error associated with a Connect peer request
                  properties:
                    code:
                      type: string
                    message:
                      type: string
                    requestID:
                      type: string
                    resourceARN:
                      type: string
                  type: object
                type: array
              state:
                description: The state of the Connect peer.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
